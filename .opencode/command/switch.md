---
description: Switch to a different mod workspace
---

Read `prompts/switch.md` and carry out that task exactly as written.

Arguments: $ARGUMENTS

The canonical instructions live in `prompts/switch.md` and the project conventions in
`AGENTS.md`. This file is only a launcher — if the two ever disagree, the prompt file wins.
