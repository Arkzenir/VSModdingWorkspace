---
description: Import an existing mod into a new workspace
---

Read `prompts/import-mod.md` and carry out that task exactly as written.

Arguments: $ARGUMENTS

The canonical instructions live in `prompts/import-mod.md` and the project conventions in
`AGENTS.md`. This file is only a launcher — if the two ever disagree, the prompt file wins.
