# Task: session status

1. Run:
   ```powershell
   .\scripts\vsmod.ps1 status
   ```
2. Report the active workspace, mod id and version, game and API versions, and which
   game source repos, dependencies and examples are linked in.
3. Note the git state of `mod/` — branch, and whether there are uncommitted changes.
4. Summarise what has been built or changed so far this session.
5. List what is still outstanding if a task is in progress.

Keep it short. This is an orientation check, not a report.
